#include "main.h"
/**
 * wildcmp - checks if string one and two are the same
 * @s1: char pointer
 * @s2: char pointer string 2
 * Return: 1 if words are identical else 0
 */
int wildcmp(char *s1, char *s2)
{
	return (0);
}
